package pl.braintelligence.requirement.task.domain.exceptions.utils;

public enum ErrorCode {

    UNEXPECTED_ERROR,
    NONEXISTENT_COUNTRY,
    NONEXISTENT_CATEGORY,
    RESOURCE_NOT_FOUND,
    API_IS_NOT_AVAILABLE,
    YOUR_API_KEY_IS_INVALID
}
